### intialize TypeScript

`npm init -y` ... Creates your package.json file.
`npm install -D typescript` ... Installs TypeScript as a dev tool.
`npx tsc --init` ... Creates your tsconfig.json file.
`npm run [script_name]` ... Runs a shortcut script from your package file.
